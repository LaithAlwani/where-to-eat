# Use a hoisted node_modules layout (npm/yarn-classic style).
# Expo/Metro (added in a later phase) resolves modules unreliably under
# pnpm's default isolated layout; hoisting is the Expo-recommended setup for
# pnpm monorepos. Convex and Next.js work fine with this layout too.
node-linker=hoisted
